spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test
actor:
  service:
    url: http://localhost:${wiremock.server.port}
  api:
    jwt:
      secretKey: dummySecretKeyForTestingPurposeOnly
mistral:
  service:
    url: http://localhost:${wiremock.server.port}
  api:
    token: dummy
security:
  jwt:
    secretKey: dummySecretKeyForTestingPurposeOnly